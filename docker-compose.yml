services:
  app:
    build: .
    environment:
      - PORT=4000
    expose:
      - '4000'
  nginx:
    image: nginx:alpine
    depends_on:
      - app
    ports:
      - '80:80'
    volumes:
      - ./nginx/default.conf:/etc/nginx/conf.d/default.conf:ro
